# Found / Filter UI change

- Removed the internal two-tab bar `Найденные / Фильтр уведомлений` from the notifications screen.
- Renamed the main green quick-action button from `Уведомления` to `Найденные`.
- The adjacent `Фильтр` button now opens the notification-filter editor directly.
