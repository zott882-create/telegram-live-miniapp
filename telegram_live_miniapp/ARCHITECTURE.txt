Telegram Mini App URL
        |
        v
combined_app.py (one Railway service)
   |                           |
   |                           +--> /notify/ and /api/*
   |                                frozen notification bot V10_03
   |
   +--> / and /api-football/*
        API-Football match centre
        Redis cache + in-memory fallback

Postgres and Redis remain shared through DATABASE_URL and REDIS_URL.
