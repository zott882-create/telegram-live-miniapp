# Main feed filter redesign

The main-page filter now uses the same restrained dark-green interface as the notification filter.

Changes:
- unified header, section cards, range inputs, condition rows and sticky actions;
- no emoji or decorative illustrations;
- all previous filter fields and element IDs are preserved;
- existing apply/reset logic is unchanged;
- stylesheet cache version was bumped so Telegram refreshes the interface.
